name: Deploy to GitHub Pages

# MAINTENANCE: auto-deploy on push is paused while the live site is being
# fixed. Re-enable by uncommenting the `push` trigger below. Until then the
# site can still be deployed manually via the "Run workflow" button
# (workflow_dispatch) in the Actions tab.
on:
  # push:
  #   branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Sync lineup data
        run: npm run lineup:sync

      - name: Build static export
        run: npm run build
        env:
          NEXT_PUBLIC_FESTIVAL_ID: sziget-2026

      - uses: actions/upload-pages-artifact@v5
        with:
          path: ./out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v5
        id: deployment
